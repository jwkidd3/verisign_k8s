apiVersion: v1
kind: Pod
metadata:
  name: privileged-test
  namespace: lab07-restricted-$STUDENT_NAME
spec:
  containers:
  - name: nginx
    image: nginx:1.25
    securityContext:
      privileged: true
