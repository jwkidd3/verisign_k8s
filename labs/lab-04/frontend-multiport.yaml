apiVersion: v1
kind: Service
metadata:
  name: frontend-multiport
  namespace: lab04-$STUDENT_NAME
spec:
  type: ClusterIP
  selector:
    app: frontend
    tier: web
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: https
      port: 8443
      targetPort: 8443
