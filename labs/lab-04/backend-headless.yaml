apiVersion: v1
kind: Service
metadata:
  name: backend-headless
  namespace: lab04-$STUDENT_NAME
spec:
  clusterIP: None
  selector:
    app: backend
    tier: api
  ports:
    - name: http
      port: 80
      targetPort: 80
