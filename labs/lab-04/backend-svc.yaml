apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: lab04-$STUDENT_NAME
spec:
  type: ClusterIP
  selector:
    app: backend
    tier: api
  ports:
    - name: http
      port: 80
      targetPort: 80
