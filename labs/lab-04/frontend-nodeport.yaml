apiVersion: v1
kind: Service
metadata:
  name: frontend-nodeport
  namespace: lab04-$STUDENT_NAME
spec:
  type: NodePort
  selector:
    app: frontend
    tier: web
  ports:
    - name: http
      port: 80
      targetPort: 80
